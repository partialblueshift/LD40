using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class NPC: Character
{
    public NPC(string name, string role, Position startingPosition) : base (name, role, startingPosition)
    {
    }
}
